name: TAUR REINVESTMENT LOG V1 (ARCHIVED)

# Money Log runtime remains active through taur-reinvestment-log-v1.js.
# Module wiring is centralized in taur-module-wiring-v2.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR money log wiring is centralized; no legacy patch required."
