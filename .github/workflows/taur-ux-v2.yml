name: TAUR UX V2 (ARCHIVED)

# UX V2 is already installed in index.html.
# Legacy injection is manual-only to prevent concurrent index.html rewrites.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR UX V2 is integrated; no legacy injector required."
