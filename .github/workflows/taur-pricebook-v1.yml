name: TAUR PRICE BOOK V1

on:
  push:
    branches: [main]
    paths-ignore:
      - index.html
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pricebook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Wire price book module
        run: |
          python3 <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          tag='<script src="taur-pricebook-v1.js"></script>'
          if tag not in s:
              if '</body>' not in s: raise SystemExit('index.html missing </body>')
              s=s.replace('</body>',tag+'</body>',1)
              p.write_text(s,encoding='utf-8')
          else:
              print('TAUR PRICE BOOK already wired')
          PY
      - name: Commit wiring
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Wire TAUR reusable price book"
          git push
