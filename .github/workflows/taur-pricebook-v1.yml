name: TAUR PRICE BOOK V1 (ARCHIVED)

# Price Book runtime remains active through taur-pricebook-v1.js.
# Module wiring is centralized in taur-module-wiring-v2.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR price book wiring is centralized; no legacy patch required."
