name: TAUR JOB FILE V2 (ARCHIVED)

# Job File runtime remains active through taur-jobfile-xip-v2.js.
# Module wiring is centralized in taur-module-wiring-v2.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR Job File wiring is centralized; no legacy patch required."
