name: TAUR CLOUD SYNC V1 (ARCHIVED)

# Cloud runtime remains active through taur-cloud-sync-v1.js.
# Module wiring is centralized in taur-module-wiring-v2.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR cloud module wiring is centralized; no legacy patch required."
