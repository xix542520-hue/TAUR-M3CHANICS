name: TAUR HOME Logbook (ARCHIVED)

# HOME is already integrated in index.html.
# Keep this workflow manual-only so legacy patch logic cannot overwrite newer app code.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR HOME Logbook is integrated; no patch required."
