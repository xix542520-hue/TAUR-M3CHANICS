name: TAUR navigation v2 (ARCHIVED)

# Navigation is already integrated in index.html.
# Legacy patching is manual-only to prevent overwrite conflicts.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TAUR navigation is integrated; no patch required."
